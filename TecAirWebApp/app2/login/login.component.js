'use strict'

angular.
    module('login').
    component('login', {
        templateUrl: 'login/login.template.html',
        controller: [
          function LoginController() {
            // this.clients = Client.query();
            // this.orderProp = 'age';
          }
        ]
      });